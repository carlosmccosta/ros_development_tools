# connect
nmcli connection up [name]

# check connections configurations
nmcli device show

# check networks interfaces
ifconfig

# check routing
route -n

# set interface priority
sudo ifmetric wlan0 50
